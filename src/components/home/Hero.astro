<section class="container hero">
  <div class="hero-blueprint blueprint-grid"></div>
  <div class="hero-content">
    <span class="tag-status">ALWAYS_INTENSIVE_LEARNING</span>
    <h1>DIGITAL<br /><span>CRAFTSMAN</span></h1>
    <p class="hero-desc">
      My name is Fernando Pastor, and I'm a seasoned professional with over 20
      years of experience in graphic design, web development, and digital
      marketing , specializing in marketing strategies.
    </p>
  </div>
  <div class="hero-meta">
    <div class="meta-item">
      <label>Base</label>
      <p>ALICANTE, SPAIN</p>
    </div>

    <div class="meta-item">
      <label>Lat / Long</label>
      <p>
        38.3579° N, 0.5137° W <a
          href="https://www.google.com/maps/@38.3473476,-0.5086511,12z"
          target="_blank"
          rel="noopener noreferrer">-></a
        >
      </p>
    </div>
    <div class="meta-item">
      <label>Current Status</label>
      <p class="status-text">
        <span class="status-dot"></span>OCCUPIED
      </p>
    </div>
  </div>
</section>

<style>
  .hero {
    display: flex;
    flex-direction: column;
    justify-content: center;
    min-height: 90vh;
    overflow: hidden;
    padding: 2rem;
    position: relative;

    & .hero-blueprint {
      @media (width < 1024px) {
        display: none;
      }

      @media (width >= 1024px) {
        display: block;
      }

      height: 100%;
      opacity: 0.1;
      pointer-events: none;
      position: absolute;
      right: 0;
      top: 5rem;
      width: 50%;
    }

    & .hero-content {
      & .tag-status {
        background: rgb(5 107 0 / 10%);
        color: var(--secondary);
        display: inline-block;
        font-family: var(--font-headline);
        font-size: 0.75rem;
        font-weight: 700;
        letter-spacing: 0.2em;
        margin-bottom: 1rem;
        padding: 0.25rem 0.75rem;
        text-transform: uppercase;
      }

      & h1 {
        font-family: var(--font-headline);
        font-size: clamp(3rem, 12vw, 10rem);
        font-weight: 700;
        letter-spacing: -0.05em;
        line-height: 0.85;
        margin-bottom: 2rem;

        & span {
          color: var(--primary);
          font-style: italic;
        }
      }

      & .hero-desc {
        border-left: 4px solid var(--primary);
        color: var(--on-surface-variant);
        font-size: 1.25rem;
        margin-bottom: 5rem;
        max-width: 40rem;
        padding-left: 1.5rem;
      }
    }
  }

  .hero-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 3rem;

    .meta-item {
      & label {
        color: var(--outline);
        display: block;
        font-family: var(--font-headline);
        font-size: 0.65rem;
        letter-spacing: 0.15em;
        margin-bottom: 0.25rem;
        text-transform: uppercase;
      }

      & p {
        font-family: var(--font-headline);
        font-size: 1.125rem;
        font-weight: 700;

        &.status-text {
          color: var(--secondary);
        }

        & .status-dot {
          animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
          background: var(--secondary);
          border-radius: 50%;
          display: inline-block;
          height: 8px;
          margin-right: 0.5rem;
          width: 8px;
        }
      }
    }
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
    }

    50% {
      opacity: 0.5;
    }
  }
</style>
