<!-- Footer -->
<footer>
  <div class="footer-content">
    <div class="footer-brand">
      <h2>fernandopastor.dev</h2>
      <p>2026 Fernando Pastor // Made with love in Alicante</p>
    </div>
    <div class="footer-links">
      <a
        href="https://github.com/fpastor"
        target="_blank"
        rel="noopener noreferrer">Github</a
      >
      <a
        href="https://www.linkedin.com/in/fernando-pastor/"
        target="_blank"
        rel="noopener noreferrer">LinkedIn</a
      >
      <!-- <span class="status">Status: Available</span> -->
    </div>
  </div>
  <div class="decoration-bar"></div>
</footer>

<style>
  footer {
    background: var(--surface-container-low);
    border-top: 1px solid rgb(var(--color-outline-rgb), 0.15);

    .footer-content {
      @media (width >= 768px) {
        grid-template-columns: 1fr 1fr;
      }

      @media (width < 768px) {
        grid-template-columns: 1fr;
      }

      display: grid;
      gap: var(--space-2xl);
      margin: 0 auto;
      max-width: var(--max-width);
      padding: var(--space-3xl) var(--container-padding);
    }

    .footer-brand {
      color: var(--on-surface-variant);
      font-family: var(--font-headline);
      font-size: 0.7rem;
      font-weight: 500;
      line-height: 0.75rem;

      h2 {
        margin-bottom: var(--space-sm);
      }

      p {
        font-weight: 400;
        letter-spacing: 0;
        text-transform: none;
      }
    }

    .footer-links {
      @media (width >= 768px) {
        justify-content: flex-end;
      }

      align-items: center;
      display: flex;
      flex-direction: row;
      gap: var(--space-md);

      .status {
        color: var(--secondary);
        font-family: var(--font-headline);
        font-size: 0.7rem;
        font-weight: 500;
        letter-spacing: 0.1em;
        text-transform: uppercase;
      }

      a {
        background-color: var(--secondary-soft);
        border-radius: var(--radius-full);
        color: var(--on-surface-variant);
        display: inline-block;
        font-family: var(--font-label);
        font-size: var(--font-size-xs);
        letter-spacing: 0.1em;
        margin: 0;
        padding: var(--space-2xs) var(--space-md);
        text-decoration: none;
        text-transform: uppercase;
        transition: color 0.3s;

        &:hover {
          background-color: var(--secondary-soft-hover);
        }
      }
    }

    .decoration-bar {
      background: linear-gradient(
        to right,
        var(--primary),
        var(--secondary),
        var(--color-accent-strong)
      );
      height: 4px;
      opacity: 0.3;
      width: 100%;
    }
  }
</style>
